import {useEffect,useState} from "react";import Sidebar from "./components/Sidebar.jsx";import Dashboard from "./pages/Dashboard.jsx";import Settings from "./pages/Settings.jsx";import Production from "./pages/Production.jsx";import Placeholder from "./pages/Placeholder.jsx";import {loadEntries,loadSettings,saveEntries} from "./services/storage.js";import "./styles/main.css";
export default function App(){const[active,setActive]=useState(localStorage.getItem("adama_bi_active_page")||"dashboard");const[settings,setSettings]=useState(loadSettings);const[entries,setEntriesState]=useState(loadEntries);const setEntries=next=>{setEntriesState(next);saveEntries(next)};useEffect(()=>localStorage.setItem("adama_bi_active_page",active),[active]);useEffect(()=>{if("serviceWorker" in navigator)navigator.serviceWorker.register("/service-worker.js").catch(()=>{})},[]);const pages={dashboard:<Dashboard entries={entries} settings={settings}/>,facilities:<Settings settings={settings} setSettings={setSettings}/>,production:<Production entries={entries} setEntries={setEntries} settings={settings}/>,quality:<Placeholder title="איכות" subtitle="מנות תקינות, חריגות, מותנה ואירועי איכות"/>,orders:<Placeholder title="הזמנות" subtitle="פתוחות, סגורות ופער מתוכנן מול נמסר"/>,analytics:<Placeholder title="מגמות וניתוח" subtitle="השוואות תקופה, תחזיות ו־OEE"/>,reports:<Placeholder title="דוחות Excel" subtitle="ייצוא דוח הנהלה, Raw Data ותרשימים"/>,whatsapp:<Placeholder title="WhatsApp" subtitle="טקסט מעוצב, תמונת הנהלה ושיתוף"/>,settings:<Settings settings={settings} setSettings={setSettings}/>,admin:<Placeholder title="ניהול והרשאות" subtitle="משתמשים, הרשאות ו־Audit Log"/>};return <div className="app-shell"><Sidebar active={active} setActive={setActive}/><main className="main-content"><header className="topbar"><div><strong>ADAMA Production BI</strong><span>מערכת ניהול ייצור, אריזה ואיכות</span></div><button onClick={()=>window.location.reload()}>רענן</button></header>{pages[active]||pages.dashboard}</main></div>}
