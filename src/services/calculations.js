export function summarizeEntries(entries,settings){const packaging=entries.reduce((s,e)=>s+Number(e.packaging||0),0);const production=entries.reduce((s,e)=>s+Number(e.production||0),0);return{packaging,production,target:0,achievement:0}}export function summarizeQuality(rows){return{total:rows.length,ok:rows.filter(x=>x.status==="ok").length,pending:rows.filter(x=>x.status==="pending").length,bad:rows.filter(x=>x.status==="bad").length}}