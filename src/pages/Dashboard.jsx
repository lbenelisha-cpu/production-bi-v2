import KpiCard from"../components/KpiCard.jsx";import{fmt}from"../services/storage.js";import{summarizeEntries,summarizeQuality}from"../services/calculations.js";export default function Dashboard({entries,quality,settings}){const s=summarizeEntries(entries,settings),q=summarizeQuality(quality);return <section className="page"><div className="page-head"><div><h2>דשבורד ראשי</h2><p>Cloud Sprint 5</p></div><div className="version-badge">Sprint 5</div></div><div className="kpi-grid"><KpiCard label="סה״כ אריזה" value={fmt(s.packaging)} tone="teal"/><KpiCard label="סה״כ ייצור" value={fmt(s.production)} tone="amber"/><KpiCard label="רשומות איכות" value={fmt(q.total)}/><KpiCard label="חריגות איכות" value={fmt(q.bad)} tone="red"/></div></section>}