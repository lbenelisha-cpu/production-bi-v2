import{useRef,useState}from"react";import{parseExcelFile}from"../services/excelImport.js";import{cloudReplaceEntries,cloudReplaceQuality}from"../services/cloudStorage.js";import{supabaseEnabled}from"../services/supabaseClient.js";export default function DataImport({entries,setEntries,quality,setQuality}){const ref=useRef(null),[msg,setMsg]=useState("");async function onFile(file){if(!file)return;try{const r=await parseExcelFile(file);if(r.type==="quality"){setQuality(r.rows);if(supabaseEnabled)await cloudReplaceQuality(r.rows)}else{setEntries(r.rows);if(supabaseEnabled)await cloudReplaceEntries(r.rows)}setMsg(r.message+(supabaseEnabled?" | נשמר בענן":" | נשמר מקומית"))}catch(e){setMsg("שגיאה: "+e.message)}}return <section className="page"><div className="page-head"><div><h2>טעינת נתונים</h2><p>טעינת Excel לענן או מקומית</p></div><div className="version-badge">{supabaseEnabled?"Cloud":"Local"}</div></div><div className="panel"><input ref={ref} type="file" accept=".xlsx,.xls,.csv" style={{display:"none"}} onChange={e=>onFile(e.target.files?.[0])}/><button className="action-btn" onClick={()=>ref.current?.click()}>בחר קובץ Excel</button><p className="import-message">{msg}</p></div></section>}